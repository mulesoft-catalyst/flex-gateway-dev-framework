apiVersion: gateway.mulesoft.com/v1alpha1
kind: PolicyBinding
metadata:
  name: test-policy
spec:
  targetRef:
    name: httpbin-example
  policyRef:
    name: test-policy-implementation
  config:
    header: testheader